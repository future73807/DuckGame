{
  "name": "duckgame",
  "version": "7.6.0",
  "description": "小黄鸭漂流记 - Web3D 休闲收集游戏",
  "main": "server.node.js",
  "scripts": {
    "start": "node server.node.js",
    "server": "node server.node.js",
    "client": "npx serve . -l 5500",
    "check:client": "node scripts/check-client.js",
    "check:server": "node --check server.node.js",
    "check:duo": "node scripts/check-duo-delta.js",
    "check:water": "node scripts/check-water-scheduler.js",
    "check:feedback": "node scripts/check-run-feedback.js",
    "check:festival": "node scripts/check-festival-fx.js",
    "check": "npm run check:client && npm run check:server && npm run check:duo && npm run check:water && npm run check:feedback && npm run check:festival",
    "deploy:prod": "node scripts/prepare-deploy.js --site 3d-duck --mode prod && deno -A https://host.retiehe.com/cli deploy --site 3d-duck --outdir .deploy/3d-duck",
    "deploy:dev": "node scripts/prepare-deploy.js --site duck-game --mode dev && deno -A https://host.retiehe.com/cli deploy --site duck-game --outdir .deploy/duck-game",
    "deploy": "npm run deploy:prod && npm run deploy:dev",
    "test": "npm run check",
    "deploy": "deno -Ar https://host.retiehe.com/cli deploy"
  },
  "keywords": [
    "game",
    "web3d",
    "three.js",
    "duck"
  ],
  "author": "future73807",
  "license": "MIT",
  "devDependencies": {
    "acorn": "^8.17.0"
  }
}
